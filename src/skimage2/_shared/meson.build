python_sources = [
  '__init__.py',
  'api.py'
]

py3.install_sources(
  python_sources,
  pure: false,                # Will be installed next to binaries
  subdir: 'skimage2/_shared'  # Folder relative to site-packages to install to
)
